<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:StarwarsAPI" x:Class="StarwarsAPI.MainPage">
    <StackLayout>
        <!-- Place new controls here -->
        <ListView x:Name="film_list">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding title}" Detail="{Binding director}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>